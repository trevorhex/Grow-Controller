export interface RoutesType {
  HOME: string
  BOUNDARIES: string
  WARNINGS: string
  PREVIOUS: string
}

export const ROUTES: RoutesType = {
  HOME: '/',
  BOUNDARIES: '/boundaries',
  WARNINGS: '/warnings',
  PREVIOUS: '/previous'
}
