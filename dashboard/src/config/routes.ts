export interface RoutesType {
  HOME: string
  WARNINGS: string
  FLUSHES: string
  BOUNDARIES: string
}

export const ROUTES: RoutesType = {
  HOME: '/',
  WARNINGS: '/warnings',
  FLUSHES: '/flushes',
  BOUNDARIES: '/boundaries'
}
